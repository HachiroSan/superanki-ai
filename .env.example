# Copy this file to .env.dev, .env.test, or .env.production and adjust values as needed

# Environment
NODE_ENV=development

# Database Configuration
# Path to the main SQLite database file
DATABASE_PATH=./data/app.db
# Path to database migration files
DATABASE_MIGRATIONS_PATH=./migrations

# File Watcher Configuration
# File pattern to watch for changes (glob pattern)
WATCH_PATTERN=*.txt
# Directory to watch for file changes
WATCH_DIRECTORY=./files
# Debounce time in milliseconds for file change events
WATCH_DEBOUNCE_MS=1000

# LLM Enrichment (3rd pipeline stage)
# Enable the LLM enrichment stage (true|false)
LLM_ENABLED=false
# LLM provider hint (e.g., openai)
LLM_PROVIDER=openai
# Model name when using provider
LLM_MODEL=gpt-4o-mini
# Batching controls
LLM_BATCH_SIZE=20
LLM_CONCURRENCY=2
# Provider API key is separate (e.g., OPENAI_API_KEY)
# OPENAI_API_KEY=sk-...

# --- OpenRouter (OpenAI-compatible) ---
# If you use OpenRouter, set these instead of the default OpenAI host.
# The OpenAI SDK will pick these up automatically.
# OPENAI_API_KEY=or_XXXXXXXXXXXXXXXXXXXXXXXXXXXX
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
# For OpenRouter models, use slugs like:
# LLM_MODEL=openai/gpt-4o-mini
# LLM_MODEL=anthropic/claude-3.5-sonnet
# Optional (recommended by OpenRouter for attribution):
# OPENROUTER_SITE_URL=https://your.site
# OPENROUTER_APP_NAME=SuperAnki AI

# Hash Algorithm
# Algorithm used for file hashing (SHA256 or xxHash)
HASH_ALGORITHM=SHA256

# Logging Configuration
# Log level (debug, info, warn, error)
LOG_LEVEL=info
