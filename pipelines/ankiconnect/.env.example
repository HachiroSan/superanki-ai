# AnkiConnect helper defaults
ANKI_CONNECT_URL=http://localhost:8765
ANKI_DECK_NAME=SuperAnki
ANKI_MODEL_NAME=Basic

# Database (defaults mirror project .env.example)
DATABASE_PATH=./data/app.db

# Optional: limit how many notes to push per run
PUSH_LIMIT=200

# Container runtime settings for docker-compose (optional)
TZ=UTC
USER_ID=1000
GROUP_ID=1000

